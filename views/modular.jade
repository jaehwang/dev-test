section
  h2.title 복잡성 다루기: 나누기와 이름 붙이기
  
  p 복잡성 다루기: 프로그래밍의 핵심 기술 
  ul
    li Abstraction
      ul
        li Function (Name): 하는 일에 이름 붙이기
        li User-Defined Data Type: 다루는 값이나 객체에 이름 붙이기
    li Modularity
      ul
        li 파일과 디렉터리에 나눠 담기

section
  h2.title 복잡성 다루기: 나누기와 이름 붙이기

  ul 
    li 나누기를 돕는 도구
      ul
        li Editor: 80 x 40
        li Compiler, Linker, Loader, Runtime, Type System, ...
        li 3rd Party Module System (e.g. CommonJS)
    li 잘 나누면 집에 일찍 갈 수 있음
      ul
        li 이해 하기 쉽고, 테스트하기 쉬움
        li 재사용 하기 쉽고, 여럿이 일을 나눠 하기 쉬움

section

  h2.title 복잡성 다루기: 나누기와 이름 붙이기
  p index.jade
  pre
    code.
      extends layout
      
      block content
        .reveal
          .slides
            section
              include title.jade
            section
              include larry_and_kent.jade
            section
              include why.jade
            section
              include hacker.jade
            section
              include responsibility.jade
            section
              h2.title Unit Test in V&V
              img(src='image/vv.png')
            section
              include bug.jade
            section
              include proof_review.jade
            section
              include static_analysis.jade
            section
              include sw_company.jade
            section
              include code_quality.jade
            section
              include modular.jade
      
            if include_shell != 0
              section
                include demo.jade
      
            section
              h2.title END
      
      block test_header
        link(rel='stylesheet', href='qunit/qunit-1.12.0.css')
        script(src='http://code.jquery.com/jquery-1.10.1.min.js');
      
      block test_footer
        script(src='qunit/qunit-1.12.0.js')
        script.
          test("Basic Structure", function() {
            $("#doc_title").each(function() {
              equal(this.innerHTML, "Testing Completes Developing", "Document Title");
            });
      
            $("section > .title").each(function() {
              equal(this.tagName, "H2", "Slide title must be H2");
            });
            $("section > h2").each(function() {
              ok($(this).hasClass('title'), "H2 under section must be title class");
            });
          });
